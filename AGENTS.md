# Expo HAS CHANGED

This project runs Expo SDK 54. Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code — Expo
changes enough between major versions that anything recalled from memory or
an older doc version risks being wrong for this project specifically.

When the project upgrades to a newer SDK, update the version number above —
it must always match the `expo` version in `package.json`.
